# 引継書: 記事作成オンラインツール Railway デプロイ経緯

作成日: 2026-05-21

## 目的

ローカルCLIで動いている「記事自動生成ツール」を、オンライン画面/APIから使える形へ移植する。

今回のA地点は、完全な本番運用完成ではなく、次の確認が通る状態。

1. 公開URLで `/healthz` が応答する
2. 公開URLで `/deployment-readiness` が確認できる
3. 安全チェックジョブが実行できる
4. 小さなAPI消費ジョブが実行できる

管理画面や作業状況確認は補助であり、主目的は「親記事作成・子記事作成・メタ情報・内部リンク判断などの記事作成自動化をオンラインへ移すこと」。

## 対象リポジトリ

GitHub:

```text
https://github.com/yk6hiop/article-auto-online.git
```

ローカル作業フォルダ:

```text
G:\マイドライブ\claude-work-shared
```

Railway公開URL:

```text
https://web-production-a67a1.up.railway.app
```

Railwayプロジェクト名:

```text
compassionate-bravery
```

Railwayサービス名:

```text
web
```

## 実施済みの主な作業

### 1. オンライン版の土台

FastAPIベースのオンラインアプリを用意。

主なファイル:

```text
online_app/app.py
online_app/deployment_readiness.py
online_app/runtime_config.py
online_app/job_store.py
online_app/worker.py
```

主な確認URL:

```text
/healthz
/deployment-readiness
/api/deployment-readiness
```

### 2. 秘密情報の分離

公開リポジトリへAPIキーやWordPressパスワードを含めないようにした。

ローカル秘密設定:

```text
G:\マイドライブ\claude-work-shared\local_private_config.json
```

Railway用env生成済みファイル:

```text
G:\マイドライブ\claude-work-shared\online_app_data\deploy_env_railway.private.txt
```

注意:

- 上記ファイルは秘密情報を含む
- Gitには入れない
- チャットにも貼らない
- RailwayのVariablesへ設定するための元データ

### 3. デプロイ用ファイル

追加・調整したファイル:

```text
Dockerfile
Procfile
requirements.txt
railway.json
.dockerignore
.gitignore
render.yaml
```

Render用の `render.yaml` もあるが、今回はRailwayでデプロイしている。

### 4. GitHub連携

GitHubリポジトリへpush済み。

主なコミット:

```text
62dfe72 Prepare online app v0 deployment
ac5dc99 Add private online env export helper
c4db615 Fix Railway startup configuration
3fcbe65 Let Dockerfile handle Railway startup
```

## Railwayで起きた問題と対応

### 問題1: Railway UIのGitHub連携

Railwayの新規プロジェクト画面では、現在のUI上で以下を選ぶ。

```text
GitHub Repository
```

GitHub Appの認可画面では、推奨は以下。

```text
Only select repositories
```

選択するリポジトリ:

```text
article-auto-online
```

### 問題2: 既存SEO Doctorプロジェクトと混同しやすい

既存プロジェクト:

```text
seo-doctor-online-staging
seo-doctor-web-production.up.railway.app
```

これは既存のSEO Doctor用。

今回の記事作成オンラインツールは別プロジェクトとして作成。

今回の新規プロジェクト:

```text
compassionate-bravery
```

既存の `Postgres` や `seo-doctor-web` は触らない。

### 問題3: Railway上でDeployment successfulでも502になった

Railway画面上で `Deployment successful` と出ても、公開URLが応答するとは限らない。

実際に以下は502だった。

```text
https://web-production-a67a1.up.railway.app/healthz
https://web-production-a67a1.up.railway.app/deployment-readiness
https://web-production-a67a1.up.railway.app/api/deployment-readiness
```

結論:

```text
Railway画面の成功表示ではなく、公開URLの /healthz を最終確認にする。
```

### 問題4: railway.json の startCommand が原因でヘルスチェック失敗

Railwayの診断:

```text
Remove the startCommand from railway.json so the Dockerfile's CMD handles startup correctly.
The start command in railway.json passes the literal string $PORT to uvicorn instead of an expanded integer.
```

原因:

`railway.json` の `startCommand` で `$PORT` が展開されず、uvicornに文字列 `$PORT` が渡ってクラッシュしていた。

対応:

```text
railway.json から startCommand を削除
Dockerfile の CMD に起動を任せる
```

対応コミット:

```text
3fcbe65 Let Dockerfile handle Railway startup
```

### 問題5: Railwayダッシュボード側に古いStart Commandが残る

`railway.json` から `startCommand` を削除しても、Railwayダッシュボード側に古いStart Commandが残っている場合がある。

Railway診断:

```text
Clear the start command in the Railway dashboard so the Dockerfile's CMD handles startup.
The dashboard has a stale start command that passes the literal string "${PORT:-8000}" to uvicorn instead of an expanded port number.
```

原因:

```text
コード上の railway.json は直っているが、Railwayサービス設定側のStart Command overrideが残っている。
そのため、DockerfileのCMDではなく、古い dashboard start command が優先される。
```

対応:

Railwayの失敗デプロイ画面に表示される以下のボタンを押す。

```text
Clear start command
```

または、Railwayのサービス設定でStart Commandを空にする。

期待される状態:

```text
Start Commandは未設定
起動はDockerfileのCMDが担当
```

Dockerfile側の起動コマンド:

```text
CMD ["sh", "-c", "uvicorn online_app.app:app --host 0.0.0.0 --port ${PORT:-8000}"]
```

この形なら `sh -c` が `${PORT:-8000}` を展開する。

## 現在の状態

2026-05-21時点で、Railway上では次のデプロイが進行中。

```text
Let Dockerfile handle Railway startup
Deployment in progress: Performing healthchecks...
```

その後、Railway側の古いStart Command overrideが残っていたため、再度ヘルスチェック失敗。

次に必要な操作:

```text
Railway画面の Clear start command を押す
```

この状態は、コード修正は反映済みだが、Railway側設定の古い起動コマンドを消す必要がある段階。

まだ完了判定ではない。

次に確認すること:

```text
https://web-production-a67a1.up.railway.app/healthz
```

これが200を返したら、続けて以下を確認する。

```text
https://web-production-a67a1.up.railway.app/deployment-readiness
https://web-production-a67a1.up.railway.app/api/deployment-readiness
```

## Railway用の必須設定

### Variables

Railwayの `Variables` には、以下のような環境変数を設定する。

値はこのファイルからコピーする。

```text
G:\マイドライブ\claude-work-shared\online_app_data\deploy_env_railway.private.txt
```

主な変数:

```text
ONLINE_APP_DATA_DIR=/app/online_app_data
ONLINE_GEMINI_KEYS_NORMAL_JSON=...
ONLINE_GEMINI_KEYS_MOECHIN_JSON=...
ONLINE_WP_SITE_OVERRIDES_JSON=...
SEARCHAPI_API_KEY=...
```

注意:

- 値は秘密情報なので、この引継書には書かない
- GitHubには絶対に入れない
- RailwayのVariablesへ貼る

### 永続保存先

ジョブ履歴などの保存用に、Railway側でVolumeを使う場合は以下へマウントする。

```text
/app/online_app_data
```

環境変数と一致させる。

```text
ONLINE_APP_DATA_DIR=/app/online_app_data
```

## 別ツールをRailwayへ上げる時の教訓

### 1. まずGitHubに秘密情報が入らない状態にする

やること:

- APIキーを環境変数化
- ローカル秘密設定ファイルを `.gitignore` に入れる
- `.dockerignore` にも秘密ファイルを入れる
- デプロイ前に秘密情報スキャンを行う

### 2. Railwayの成功表示を信用しすぎない

Railwayの `Deployment successful` は、アプリの公開動作確認ではない。

必ず公開URLで確認する。

```text
/healthz
/deployment-readiness
```

### 3. Railwayでは起動コマンドの扱いに注意

Dockerfileを使う場合は、基本的にDockerfileの `CMD` に任せる。

今回のように `railway.json` の `startCommand` で `$PORT` を渡すと、展開されずにクラッシュする場合がある。

今回の正しい方針:

```text
railway.json には healthcheckPath などだけを書く
起動は Dockerfile の CMD に任せる
```

### 4. ログを見る場所

確認順:

1. Deployments
2. 最新デプロイ
3. View Logs
4. Buildログではなく、起動後のRuntime/Deployログを見る

見るべき語句:

```text
Traceback
ModuleNotFoundError
RuntimeError
Healthcheck failure
Port
uvicorn
Exited
```

### 5. 新旧デプロイの取り違えに注意

Railway画面では、古い成功デプロイが上に残って見えることがある。

確認するコミット名:

```text
Let Dockerfile handle Railway startup
```

古いデプロイ名:

```text
Add private online env export helper
Fix Railway startup configuration
```

古いデプロイがActiveでも、最新修正が反映されているとは限らない。

## 次にやること

1. Railwayで `Let Dockerfile handle Railway startup` のデプロイ完了を待つ
2. 公開URLで `/healthz` を確認
3. `/deployment-readiness` を確認
4. VariablesとVolume設定の不足を潰す
5. 安全チェックジョブを実行
6. 小さなAPI消費ジョブを実行
7. A地点到達判定を行う

## 重要な注意

ここまでの作業で、オンライン版の土台はRailwayへ載り始めている。

ただし、この記事作成オンラインツールが完成したかどうかは、Railwayのデプロイ成功表示ではなく、公開URLでの実動作確認で判定する。

完了判定は次の4つ。

```text
/healthz が200
/deployment-readiness がエラー0
安全チェックジョブ成功
小さなAPI消費ジョブ成功
```
